#!/bin/bash

# 这个文件用于覆盖原来的 pandoc, 添加上自定义 filter

ORIGIN_PANDOC=/usr/bin/pandoc

# --no-highlight 阻止 pandoc 自带的语法高亮, filter 里会用 prismjs 处理.
exec $ORIGIN_PANDOC $@ --no-highlight \
  -f markdown+autolink_bare_uris+auto_identifiers+emoji --filter=pandoc-filter
